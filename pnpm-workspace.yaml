packages:
  - "packages/*"
  - "apps/*"

allowBuilds:
  # tsx and Vitest use esbuild to transpile TypeScript. Permit this known,
  # pinned dependency so `pnpm install` works without an interactive prompt.
  esbuild: true
